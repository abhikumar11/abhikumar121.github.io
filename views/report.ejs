<!DOCTYPE html>
<html lang="en">

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
    <title>
        <%= title %>
    </title>
</head>

<body>
     <form>
        <a class="btn btn-primary" href="/download">Download</a>
    </form>
    <table class="table align-middle mb-0 bg-white">
        <thead class="bg-light">
            <tr>
                <th>Student Name</th>
                <th>College</th>
                <th>Status</th>
                <th>Final Score</th>
                <th>Company</th>
                <th>Result</th>
            </tr>
        </thead>
        <tbody>
            <%for(stu of data){%>
                <tr>
                    <td>
                        <div class="d-flex align-items-center">
                            <div class="ms-1">
                                <p class="fw-bold mb-1"><%=stu.name%></p>
                                <p class="text-muted mb-0"><%=stu.email%></p>
                            </div>
                        </div>
                    </td>
                    <td>
                        <p class="fw-normal mb-1"><%=stu.college%></p>
                        <p class="text-muted mb-0">Batch: <%=stu.batch%></p>
                    </td>
                    <td>
                        <p class="fw-normal mb-1"><%=stu.placementstatus%></p>
                
                    </td>
                    <td>
                        <p class="fw-normal mb-1"><%=(stu.dsascore+stu.webdevscore+stu.reactscore)%>/300</p>
                        <p class="text-muted mb-0">DSA: <%=stu.dsascore%> Web Dev: <%=stu.webdevscore%> React: <%=stu.dsascore%></p>
                    </td>
                        <%if(stu.interviews.length > 0){
                            for(let interview of stu.interviews)
                            {%>
                                <td>
                        <p class="fw-normal mb-1"><%=interview.company%></p>
                        <p class="text-muted mb-0">Interview Date: <%=interview.date.toString()%></p>
                    </td>
                    <td>
                        <p class="fw-normal mb-1"><%=interview.result%></p>
                    </td>
                    <%}%>
                <%}%>
                </tr>
                <%}%>
        </tbody>
    </table>
</body>

</html>